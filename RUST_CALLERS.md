# Rust callers — `async-trait-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `async-trait` | `async_trait` | 44 | 20 | 3 | 41 |

## `async-trait`

### Inbound symbols (1 distinct, 44 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `async-trait`.

#### `async_trait::async_trait` — 44 site(s) (41 codex, 3 other, 20 caller crates)

- ✅ `codex-kotlin/codex-agent-graph-store` `src/local.rs:1` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-agent-graph-store` `src/store.rs:1` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `src/auth.rs:1` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `src/endpoint/compact.rs:69` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `src/endpoint/memories.rs:74` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `src/endpoint/models.rs:81` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `src/endpoint/realtime_call.rs:229` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `tests/clients.rs:6` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-api` `tests/sse_end_to_end.rs:5` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-app-server` `src/message_processor.rs:41` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-app-server` `src/request_processors/device_key_processor.rs:9` — `use async_trait::async_trait`
- ✅ `codex-kotlin/codex-async-utils` `src/lib.rs:1` — `use async_trait::async_trait`
- _… and 32 more site(s)._
