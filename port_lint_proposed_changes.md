# port-lint Proposed Changes

**Generated:** 2026-06-21
**Source:** tmp/async-trait
**Target:** src/commonMain

These are review proposals only. They are emitted when a Rust -> Kotlin pair matches only after fallback normalization, so the existing `port-lint` header is not an exact provenance match.

| Target file | Current header | Proposed header | Source path | Reason |
|-------------|----------------|-----------------|-------------|--------|
| `kotlin/io/github/kotlinmania/asynctrait/Bound.kt` | `// port-lint: source bound.rs` | `// port-lint: source bound.rs` | `bound.rs` | `port-lint provenance header matched only after fallback normalization: 'bound.rs' vs expected 'bound.rs'` |
